let array_preguntes = [];
let prova=document.getElementById("missatge");
array_preguntes[["Qui va dirigir la pel·lícula 'El Padrino'?  a) Francis Ford Coppola "]];
for (let index = 0; index < array_preguntes.length; index++) {
    prova.innerHTML+=array_preguntes[i];
    console.log(array_preguntes[i]);
}